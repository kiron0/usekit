{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Optional schema reference for the generated registry item."
    },
    "name": {
      "type": "string",
      "description": "The name of the hook. This is used to identify the hook in the registry. It should be unique for your registry."
    },
    "type": {
      "type": "string",
      "enum": ["registry:hook"],
      "description": "The type of the item. Must be 'registry:hook' for useKit hooks."
    },
    "description": {
      "type": "string",
      "description": "The description of the hook. This is used to provide a brief overview of the hook."
    },
    "title": {
      "type": "string",
      "description": "The human-readable title for your hook. Keep it short and descriptive."
    },
    "dependencies": {
      "type": "array",
      "description": "An array of NPM dependencies required by the hook.",
      "items": {
        "type": "string"
      }
    },
    "devDependencies": {
      "type": "array",
      "description": "An array of NPM dev dependencies required by the hook.",
      "items": {
        "type": "string"
      }
    },
    "registryDependencies": {
      "type": "array",
      "description": "An array of registry hooks that this hook depends on. Use the name of the hook to reference other hooks in the registry.",
      "items": {
        "type": "string"
      }
    },
    "files": {
      "type": "array",
      "description": "The main payload of the registry hook. Supports either a legacy single file entry or a dual-language variants entry.",
      "items": {
        "oneOf": [
          {
            "type": "object",
            "properties": {
              "path": {
                "type": "string",
                "description": "The path to the file relative to the registry root."
              },
              "content": {
                "type": "string",
                "description": "The content of the file."
              },
              "target": {
                "type": "string",
                "description": "Optional install target path inside the consumer project."
              },
              "type": {
                "type": "string",
                "enum": ["registry:hook"],
                "description": "The type of the file. Must be 'registry:hook' for useKit hooks."
              }
            },
            "required": ["path", "type"],
            "additionalProperties": false
          },
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "enum": ["registry:hook"],
                "description": "The type of the file. Must be 'registry:hook' for useKit hooks."
              },
              "variants": {
                "type": "object",
                "description": "Language-specific file variants. Both ts and js must be provided.",
                "properties": {
                  "ts": {
                    "type": "object",
                    "properties": {
                      "path": {
                        "type": "string",
                        "description": "The path to the TypeScript file relative to the registry root."
                      },
                      "content": {
                        "type": "string",
                        "description": "The TypeScript file content."
                      },
                      "target": {
                        "type": "string",
                        "description": "Optional TypeScript install target path inside the consumer project."
                      }
                    },
                    "required": ["path"],
                    "additionalProperties": false
                  },
                  "js": {
                    "type": "object",
                    "properties": {
                      "path": {
                        "type": "string",
                        "description": "The path to the JavaScript file relative to the registry root."
                      },
                      "content": {
                        "type": "string",
                        "description": "The JavaScript file content."
                      },
                      "target": {
                        "type": "string",
                        "description": "Optional JavaScript install target path inside the consumer project."
                      }
                    },
                    "required": ["path"],
                    "additionalProperties": false
                  }
                },
                "required": ["ts", "js"],
                "additionalProperties": false
              }
            },
            "required": ["type", "variants"],
            "additionalProperties": false
          }
        ]
      }
    }
  },
  "required": ["name", "type"],
  "additionalProperties": false
}
